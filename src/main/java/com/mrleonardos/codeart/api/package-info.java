/**
 * Публичный интерфейс CodeArt: против него компилируются чужие моды.
 *
 * <p>
 * Здесь нет ни одного типа игры и ни одной ссылки на собственную реализацию. Api уезжает отдельным
 * джаром, и класс, который дотянулся до {@code internal} или {@code platform}, соберётся у нас и упадёт
 * у того, кто взял только api. Стережёт это тест-гейт, а не договорённость.
 *
 * <p>
 * Отсюда берут: определение арта и реестр, шов хранилища с регистрацией в {@link com.mrleonardos.codeart.api.ArtApi}
 * и хеш-утилиты для провайдеров, которые считают содержимое так же, как встроенное хранилище.
 */
package com.mrleonardos.codeart.api;
